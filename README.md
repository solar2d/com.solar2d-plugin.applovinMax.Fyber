Sources for the plugin `plugin.applovinMax.Fyber`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Fyber" = {
            publisherId = "com.solar2d",
        },
    },
}
```
